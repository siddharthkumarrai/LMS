# Node.js .gitignore

# Dependencies
# This is the most important line. It ignores the massive folder of packages
# that can be reinstalled by running `npm install` or `yarn install`.
/node_modules

# Environment Variables
# NEVER commit your .env file. It contains secret keys and passwords.
# A .env.example file can be committed to show what variables are needed.
.env
.env.local
.env.*.local

# Logs
# Log files are generated during runtime and should not be versioned.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build Output
# If you are using TypeScript or a bundler, ignore the compiled output directory.
/dist
/build
/out

# Coverage Reports
# Test coverage reports are generated files.
/coverage

# IDE & OS Files
# Ignores configuration files and junk generated by editors and operating systems.
.vscode/
.idea/
.DS_Store
Thumbs.db